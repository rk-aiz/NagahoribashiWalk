<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="com.example.nagahoribashi_walk.repository.UserMapper">
	
	<select id="findByUsername" resultType="com.example.nagahoribashi_walk.entity.User">
		SELECT id, username, password, email, role, display_name, deleted_at, created_at, updated_at, enabled
		FROM users
		WHERE username = #{username}
	</select>
	
    <!-- ページネーション付き取得: 1対多の結合があるためサブクエリで絞り込む -->
    <select id="findWithPaging" resultType="com.example.nagahoribashi_walk.entity.User">
        SELECT id, username, password, email, role, display_name, deleted_at, created_at, updated_at, enabled
		FROM users
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- ユーザー数のカウント -->
    <select id="count" resultType="long">
        SELECT count(*) FROM users
    </select>

    <!-- ユーザーの存在チェック -->
    <select id="exists" resultType="boolean">
        SELECT EXISTS (SELECT 1 FROM users WHERE username = #{username})
    </select>

</mapper>
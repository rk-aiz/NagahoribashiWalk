<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.nagahoribashi_walk.repository.SubCategoryMapper">
    
    <!-- 新規追加 -->
    <insert id="insertSubCategory" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sub_categories (name, category_id, display_order)
        VALUES (
            #{name}, 
            #{categoryId}, 
            (SELECT COALESCE(MAX(display_order), 0) + 1 FROM sub_categories)
        )
    </insert>

    <!-- 削除 -->
    <delete id="deleteSubCategory">
        DELETE FROM sub_categories WHERE id = #{id}
    </delete>

    <select id="findById" resultType="com.example.nagahoribashi_walk.dto.NavSubCategory">
        SELECT id, name, category_id FROM sub_categories
        WHERE id = #{id}
    </select>

    <select id="findSiblings" resultType="com.example.nagahoribashi_walk.dto.NavSubCategory">
        SELECT sc.id, sc.name, sc.category_id
        FROM categories c
        RIGHT JOIN sub_categories sc
        ON c.id = sc.category_id
        WHERE c.id = (SELECT category_id FROM sub_categories WHERE id = #{subCategoryId})
        ORDER BY sc.display_order ASC NULLS LAST
    </select>

    <select id="findByCategoryId" resultType="com.example.nagahoribashi_walk.dto.NavSubCategory">
        SELECT id, name, category_id
        FROM sub_categories
        WHERE category_id = #{categoryId}
        ORDER BY display_order ASC NULLS LAST
    </select>
</mapper>
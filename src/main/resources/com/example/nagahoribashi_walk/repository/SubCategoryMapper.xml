<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.nagahoribashi_walk.repository.SubCategoryMapper">


<!-- 大谷記載 -->
   <select id="findByCategoryId" resultType="com.example.nagahoribashi_walk.entity.SubCategory">
    SELECT 
        id, 
        category_id AS categoryId, 
        name, 
        display_order AS displayOrder
    FROM 
        sub_categories
    WHERE 
        category_id = #{categoryId}
    ORDER BY 
        display_order ASC
   </select>
   
    
    <!-- 新規追加 -->
    <insert id="insertSubCategory" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sub_categories (category_id, name, display_order)
        VALUES (#{categoryId}, #{name}, #{displayOrder})
    </insert>

    <!-- 削除 -->
    <delete id="deleteSubCategory">
        DELETE FROM sub_categories WHERE id = #{id}
    </delete>

</mapper>